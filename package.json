{
  "name": "papilio-loader",
  "private": true,
  "version": "0.1.0",
  "description": "Unified Papilio Loader — one flasher-core + UI, packaged as web static build and Electron desktop app",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/flasher-core && npm run build --workspace=apps/web",
    "test": "npm run test --workspace=packages/flasher-core",
    "dev:web": "npm run build --workspace=apps/web && npm run serve --workspace=apps/web",
    "dev:desktop": "npm run start --workspace=apps/desktop"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
