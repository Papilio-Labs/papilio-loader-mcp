# All Python dependencies are managed in pyproject.toml
# Install with: uv pip install -e .
#
# esptool (official) is used for ESP32 programming (safe and stable)
# pesptool (GadgetFactory fork) is used for FPGA programming via tools/pesptool submodule
